rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {
    // TEMP: allow any signed-in user full access to Firestore
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}